{{>layout/header}}

    <h1>스프링부트로 시작하는 배포 자동화된 웹 서비스 Ver.3</h1>
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-6">
                <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>
                {{! 머스테치는 if 문을 제공하지 않는다. true/false 만 판단한다. }}
                {{#userName}}
                    Logged in as: <span id="user">{{userName}}</span>
                    {{! logout 에 해당하는 컨트롤러 만들 필요 x , 스피링 시큐리티에서 기본적으로 제공 }}
                    <a href="/logout" class="btn btn-info active" role="button">Logout</a>
                {{/userName}}
                {{! ^: 값이 만약 없다면 이라는 뜻 }}
                {{^userName}}
                {{! 스피링 시큐리티에서 기본적으로 제공하는 로그인 URL , 별도의 컨트롤러 만들 필요 x }}
                    <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
                    {{! oauth2 authorization까지는 고정이고 마지막 Path 만 각 소셜 로그인 코드를 사용하면 된다. }}
                    <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
                {{/userName}}
            </div>
        </div>
        <br>
        <!-- 목록 출력 영역 -->
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>게시글번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
            </thead>
            {{! java 의 for문과 같은 문법 }}
            <tbody id="tbody">
            {{#posts}}
                <tr>
                    <td>{{id}}</td>
                     {{! 'List 에서 뽑아낸 객체' 의 필드를 사용 }}
                    <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                    <td>{{author}}</td>
                    <td>{{modifiedDate}}</td>
                </tr>
            {{/posts}}
            </tbody>
        </table>
    </div>

{{>layout/footer}}